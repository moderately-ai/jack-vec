[project]
name = "jack-vec-benchmark-tools"
version = "0.0.0"
description = "Reproducible reporting and visualization tools for JackVec benchmarks."
requires-python = "==3.11.*"
dependencies = [
    "matplotlib==3.11.0",
]

[tool.uv]
package = false
